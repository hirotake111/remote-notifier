version: 2

project_name: remote-notifier

builds:
  - builder: rust
    tool: cross
    command: build
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu

archives:
  - formats: [tar.gz]
