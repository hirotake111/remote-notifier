version: 2

project_name: remote-notifier

builds:
  - binary: remote-notifier
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz

brews:
  - repository:
      owner: hirotake111
      name: homebrew-tap
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
